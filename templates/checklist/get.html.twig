{% extends 'base.html.twig' %}

{% block title %}List All Todoes{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-body">
            <p class="card-text">{{ todo.text }}</p>
        </div>
        <div class="card.body">
            <a class="btn btn-danger float-end" href="{{ path('checklist_delete', {'id': todo.id}) }}" role="button">
                Delete
            </a>
        </div>
    </div>
{% endblock %}


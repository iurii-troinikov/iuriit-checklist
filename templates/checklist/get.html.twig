{% extends 'base.html.twig' %}

{% block title %}List All Todoes{% endblock %}

{% block body %}

    <div class="card">
        <div class="card-body">
            <p class="card-text">{{ todo.text }}</p>
        </div>
    </div>

{% endblock %}


{% extends 'base.html.twig' %}

{% block title %}List All Notes{% endblock %}

{% block body %}
            <h4>
                <a href="{{ path('checklist_get', {'categoryId': note.category_id, 'noteId': note.id}) }}">
                    {{ note.title }}
                </a>
            </h4>
            <p>{{ note.text }}</p>

{% endblock %}


<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>
            {% block title %}
                Welcome!
            {% endblock %}
        </title>
        {% block stylesheets %}
        {% endblock %}
        {% block javascripts %}
        {% endblock %}
    </head>
    <body>
    <section class="header">
    <ul class="menu">
        <li>
            <a href="{{ path('checklist_list_by_checklist', {'checklistId': 1}) }}">My summer weekends</a>
            <a href="{{ path('checklist_list_by_checklist', {'checklistId': 2}) }}">My favorite books reviews</a>
            <a href="{{ path('checklist_list_by_checklist', {'checklistId': 3}) }}">My friends hobbies</a>
        </li>
    </ul>
    </section>
    {% block body %}{% endblock %}
    </body>
</html>
